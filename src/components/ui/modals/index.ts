/**
 * Index for modals
 * Central export point for modal components
 */

export { FormModal, type FormModalProps, type FormField } from './FormModal';
export { default as CustomModal } from './CustomModal';
export { default as CustomDialog } from './CustomDialog';
